export type ContactProps = {
    name: string,
    email: string,
    service: string,
    message: string
}

export type ServiceProps = {
    title: string,
    desc: string,
    img: string,
    points: string
}